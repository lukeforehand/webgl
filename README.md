# Run local WebGL program

## build
```
npm install
```

## start local server watching for code changes
```
npm run watch &
(cd dist && python -m SimpleHTTPServer 8000)
```

## run locally
```
open http://localhost:8000
```

